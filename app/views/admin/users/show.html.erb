<div class = "container">
  <% if @user == current_user %>
     <div class = "title text-center">マイページ</div>
  <% else %>
    <div class = "title text-center"><%= @user.nickname %>さんのマイページ</div>
  <% end %>

  <div class = "card">
    <div class = "text-center">
      <!-- profile_image -->
      <!-- @user.nickname -->
      <!-- @user.recipe.count -->
      <!-- @user.followings.count -->
      <!-- @user.followers.count-->
      <!-- @user.introduction -->
      <div>
        <div class = "py-2">
          <%#= link_to "プロフィールを編集する", edit_user_path(@user) , class:"btn btn-outline-secondary btn-block"%>
        </div>
        <div class = "py-2">
          <%#= link_to "ブックマークした投稿はこちら", recipe_bookmarks_path(@user.id) , class:"btn btn-outline-secondary btn-block"%>
        </div>
      </div>


    </div>
  </div>

  <div class = "sub_title text-center py-4">投稿一覧</div>
  <div class = "row">
    <div class = "col-2"></div>
    <div class = "col-8">
      <%# @recipes.each do |recipe| %>
        <%#= render "public/recipes/index", recipe: recipe %>
      <% end %>
      <div class = "pagination-sm pt-3">
        <%#= paginate @recipes %>
      </div>
    </div>
  </div>
</div>